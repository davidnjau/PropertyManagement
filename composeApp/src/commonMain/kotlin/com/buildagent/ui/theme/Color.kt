package com.buildagent.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand — Electric Indigo ───────────────────────────────────────────────────
val Brand600 = Color(0xFF6366F1)   // indigo-500
val Brand700 = Color(0xFF4F46E5)   // indigo-600
val Brand800 = Color(0xFF3730A3)   // indigo-800
val Brand50  = Color(0xFFEEF2FF)   // indigo-50
val Brand100 = Color(0xFFE0E7FF)   // indigo-100

// ── Accent — Electric Cyan ────────────────────────────────────────────────────
val Cyan500  = Color(0xFF06B6D4)   // cyan-500  (highlights / badges)
val Cyan100  = Color(0xFFCFFAFE)   // cyan-100

// ── Status ────────────────────────────────────────────────────────────────────
val Success600 = Color(0xFF10B981)  // emerald-500
val Success100 = Color(0xFFD1FAE5)  // emerald-100
val Warning600 = Color(0xFFF59E0B)  // amber-400
val Warning100 = Color(0xFFFEF3C7)  // amber-100
val Danger600  = Color(0xFFEF4444)  // red-500
val Danger100  = Color(0xFFFEE2E2)  // red-100

// ── Neutrals — Slate scale (cooler/techy) ─────────────────────────────────────
val Gray900    = Color(0xFF0F172A)  // slate-900
val Gray700    = Color(0xFF334155)  // slate-700
val Gray500    = Color(0xFF64748B)  // slate-500
val Gray300    = Color(0xFFCBD5E1)  // slate-300
val Gray100    = Color(0xFFF1F5F9)  // slate-100
val Gray50     = Color(0xFFF8FAFC)  // slate-50
val White      = Color(0xFFFFFFFF)

// ── Sidebar ───────────────────────────────────────────────────────────────────
val Sidebar            = Color(0xFF0D0D1E)  // deep space dark
val SidebarSelected    = Color(0xFF312E81)  // indigo-900 — selected row bg
val SidebarText        = Color(0xFF94A3B8)  // slate-400
val SidebarDivider     = Color(0xFF1E293B)  // slate-800
val SidebarLabel       = Color(0xFF6366F1)  // indigo accent for section labels
