ktor {
    deployment {
        port = 3001
    }
    application {
        modules = []
    }
}

auth0 {
    domain = "test.auth0.com"
    audience = "test-audience"
    namespace = "https://buildagent.test/"
}

jwt {
    secret = "test-secret-for-routes"
}

database {
    url = "jdbc:h2:mem:buildagent_test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE"
    user = "sa"
    password = ""
}

kafka {
    bootstrapServers = "localhost:9092"
    groupId = "buildagent-test"
}

redis {
    host = "localhost"
    port = 6379
}
